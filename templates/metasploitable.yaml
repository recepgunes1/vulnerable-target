id: metasploitable2

info:
  name: metasploitable2
  author: hhsteam
  description: |
    Metasploitable is a well-known intentionally vulnerable virtual machine designed for training, exploit testing, and general security practice.
  references:
    - https://docs.rapid7.com/metasploit/metasploitable-2/
  technologies:
    - ubuntu
    - postgres
    - mysql
    - php
  tags:
    - metasploitable
    - smtp
    - ftp

providers:
  docker:
    ports:
      "8080/tcp": "80"
      "21/tcp": "21"

    content: |
      FROM php:7.4-apache
      RUN echo hello

